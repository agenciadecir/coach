{
  "regions": ["iad1"],
  "functions": {
    "src/app/api/**": {
      "maxDuration": 30
    }
  },
  "buildCommand": "prisma generate && prisma migrate deploy && next build",
  "installCommand": "bun install",
  "framework": "nextjs"
}
